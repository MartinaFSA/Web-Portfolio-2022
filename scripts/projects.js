function mostrarOverlay(i) {
    let overlay = i.getElementsByTagName('div')[1];
    overlay.style.display = 'block';
} function esconderOverlay(i) {
    let overlay = i.getElementsByTagName('div')[1];
    overlay.style.display = 'none';
}